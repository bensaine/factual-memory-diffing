[project]
name = "factual-memory-diffing"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "openai>=2.6.1",
    "pyyaml>=6.0.3",
    "tiktoken>=0.12.0",
]
